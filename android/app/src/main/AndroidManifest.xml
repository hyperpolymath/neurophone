<?xml version="1.0" encoding="utf-8"?>
<!--
SPDX-License-Identifier: MPL-2.0
SPDX-FileCopyrightText: 2025 Jonathan D.A. Jewell
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ai.neurophone">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Features (optional, for sensor availability checks) -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.light" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.proximity" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        android:extractNativeLibs="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Voice / share entry point -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="text/plain" />
                <data android:scheme="neurophone" />
            </intent-filter>
        </activity>

        <!--
        Gossamer entry-point Activity (epic #83, RFC #97, sub-issue #109).
        ai.neurophone.gossamer.NeurophoneMainActivity extends
        io.gossamer.GossamerActivity (webview shell). Left commented in
        sub-PR #3 because the Gossamer Android library is not yet resolvable
        (see app/build.gradle.kts) — registering it now would fail manifest
        merge / class resolution. The legacy .MainActivity above stays the
        LAUNCHER for now.

        TODO(#83 sub-PR #4): once the gossamer dependency is wired, enable this,
        move the LAUNCHER intent-filter here, and retire .MainActivity.

        <activity
            android:name="ai.neurophone.gossamer.NeurophoneMainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->

        <!-- Long-running foreground service that owns the sensor → LSM → ESN loop -->
        <service
            android:name=".NeurophoneService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot receiver: restart the foreground service on phone reboot.
             Hand-written Java shim (Gossamer migration, epic #83); class name
             unchanged so this registration is migration-transparent.
             TODO(#83 rebase): align with sub-PRs #4/#5 once they merge. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Quick-query receiver used by the home-screen widget tap -->
        <receiver
            android:name=".widget.NeurophoneWidgetActions"
            android:exported="false" />

        <!-- Home-screen App Widget -->
        <receiver
            android:name=".widget.NeurophoneAppWidget"
            android:exported="true"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="ai.neurophone.widget.ACTION_REFRESH" />
                <action android:name="ai.neurophone.widget.ACTION_QUERY" />
                <action android:name="ai.neurophone.widget.ACTION_TOGGLE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/neurophone_widget_info" />
        </receiver>

    </application>

</manifest>
