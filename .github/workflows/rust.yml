name: Rust

on:
  push:
    branches: [ "claude/sensor-data-bridge-01Xc6NQaZkBtqXznydxooNuw" ]
  pull_request:
    branches: [ "claude/sensor-data-bridge-01Xc6NQaZkBtqXznydxooNuw" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
